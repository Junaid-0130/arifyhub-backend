# ============================================
# FIREBASE CONFIGURATION (OPTIONAL)
# ============================================
# Firebase is optional - server will use JSON file storage if not configured.
# File uploads require Firebase Storage, but URL uploads work without Firebase.
# Choose ONE of the following options (or leave empty to use JSON storage):

# ============================================
# OPTION 1: Service Account File Path (RECOMMENDED)
# ============================================
# 1. Go to Firebase Console: https://console.firebase.google.com/
# 2. Select your project → Project Settings → Service Accounts
# 3. Click "Generate New Private Key"
# 4. Save the downloaded JSON file as: firebase-service-account.json
# 5. Place it in the project root directory
# 6. Set the path below:
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# ============================================
# OPTION 2: Service Account JSON as Environment Variable
# ============================================
# Alternative: Paste the entire service account JSON content here
# (as a single line, escape quotes properly)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# ============================================
# OPTION 3: Default Credentials (Firebase Hosting/Cloud Run)
# ============================================
# If deploying to Firebase Hosting or Cloud Run, you can use default credentials
# Just set FIREBASE_STORAGE_BUCKET below (no service account needed)

# ============================================
# FIREBASE STORAGE BUCKET (REQUIRED)
# ============================================
# Your Firebase Storage bucket name
# Format: your-project-id.appspot.com
# Find it in: Firebase Console → Storage → Files
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# ============================================
# SERVER CONFIGURATION
# ============================================
# Server port (default: 3000)
PORT=3000
